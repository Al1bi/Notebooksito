#include <bits/stdc++.h>
#define endl '\n'
#define all(a) (a).begin(), (a).end()

using namespace std;

int main(){

    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    // this is a test
    
    return 0;
}